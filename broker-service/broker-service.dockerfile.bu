FROM alpine:latest

RUN mkdir /app

# # Do not use C lib
# RUN CGO_ENABLE=0 go build -o brokerApp ./cmd/api

# # Set executable
# RUN chmod +x /app/brokerApp

COPY brokerApp /app


# Executive with escapse string
CMD ["/app/brokerApp"]
